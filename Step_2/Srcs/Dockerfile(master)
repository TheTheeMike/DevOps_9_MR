FROM jenkins/jenkins:lts

USER root

# Install docker client (docker.io) and curl
RUN apt-get update \
 && apt-get install -y docker.io curl \
 && rm -rf /var/lib/apt/lists/*

# Allow jenkins user to use docker client
RUN usermod -aG docker jenkins

USER jenkins